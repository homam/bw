const React = require('react')

module.exports = (props)=> {

    return (
        <div className="button-component">
            <button className="button round">Play now</button>
        </div>
    )
}
